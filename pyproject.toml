[project]
name = "data-registry-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pandas>=3.0.5",
    "psycopg[c]>=3.3.5",
    "python-multipart>=0.0.32",
]
